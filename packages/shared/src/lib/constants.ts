export const defaultClock24HMode = true
export const defaultScientificMode = true
export const defaultDualMode = false

export const sensorOpts = { activationConstraint: { delay: 250, tolerance: 5 } }
